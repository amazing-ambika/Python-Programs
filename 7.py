# program to get the largest number from a list by using max and min commands

example_list = [2, 7, 27]
print(max(example_list)) # max() finds the maximum among values
print(min(example_list)) # min() finds the minimum among values